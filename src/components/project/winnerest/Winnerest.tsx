import { Layout } from '../../../common/CommonStyled';
import { colors } from '../../../styles/colors';

const Winnerest = () => {
	return (
		<Layout color={colors.blue1}>
			<div>winnerrest</div>
		</Layout>
	);
};

export default Winnerest;
