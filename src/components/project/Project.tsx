import { Layout } from '../../common/CommonStyled';
import { colors } from '../../styles/colors';

const Project = () => {
	return (
		<Layout color={colors.blue3}>
			<div>project</div>
		</Layout>
	);
};

export default Project;
