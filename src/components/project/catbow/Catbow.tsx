import { Layout, Title } from '../../../common/CommonStyled';
import { colors } from '../../../styles/colors';

const Catbow = () => {
	return (
		<Layout color={colors.blue1}>
			<Title itemRef='220%'>project</Title>
		</Layout>
	);
};

export default Catbow;
